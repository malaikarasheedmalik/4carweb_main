'use client';
import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import { LockKeyhole, Wrench } from 'lucide-react';
export default function LoginPage() { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const router = useRouter(); async function submit(e: FormEvent) { e.preventDefault(); setError(''); const res = await fetch('/api/auth/login', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ email, password }) }); const json = await res.json(); if (!res.ok) return setError(json.error || 'Unable to sign in'); router.push('/admin'); } return <div className="flex min-h-screen items-center justify-center bg-[#3c2521] px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-8 shadow-2xl"><div className="flex items-center gap-3 text-[#3c2521]"><span className="rounded-lg bg-[#3c2521] p-3 text-white"><Wrench /></span><div><h1 className="font-display text-2xl font-bold">Admin sign in</h1><p className="text-sm text-[#886b60]">Manage your FixPoint website</p></div></div>{error && <p className="mt-6 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<label className="mt-6 block text-sm font-semibold text-[#3c2521]">Email<input className="mt-2 w-full rounded-lg border border-[#b6a69f] px-3 py-2.5" type="email" required value={email} onChange={e => setEmail(e.target.value)} /></label><label className="mt-4 block text-sm font-semibold text-[#3c2521]">Password<input className="mt-2 w-full rounded-lg border border-[#b6a69f] px-3 py-2.5" type="password" required value={password} onChange={e => setPassword(e.target.value)} /></label><button className="mt-6 flex w-full items-center justify-center gap-2 rounded-lg bg-[#6a4b40] py-3 font-semibold text-white hover:bg-[#4b322e]"><LockKeyhole className="h-4 w-4" />Sign in</button></form></div>; }
